var dependencies = {};
/*
dependencies['port'] = 8080;
dependencies['io'] = require('socket.io')(server);
dependencies['socket'] = require('socket.io-client')('http://localhost:'+port);
console.log(dependencies);*/

module.exports = dependencies;
